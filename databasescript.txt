CREATE DATABASE jiveapp;
USE jiveapp;
CREATE TABLE environment(
os VARCHAR(30) NOT NULL,
os_version VARCHAR(30) NOT NULL,
notes VARCHAR(200),
env_id INT UNSIGNED NOT NULL AUTO_INCREMENT PRIMARY KEY
);